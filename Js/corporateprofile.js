 // Duplicate content dynamically for seamless infinite scrolling
 const scrollContainer = document.getElementById("scroll-container");
 scrollContainer.innerHTML += scrollContainer.innerHTML; // Duplicate content